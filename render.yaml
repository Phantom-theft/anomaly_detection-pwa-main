services:
  - type: static
    name: anomaly-detection-pwa
    buildCommand: npm run build
    publishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
