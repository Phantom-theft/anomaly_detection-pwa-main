services:
  - type: web
    name: anomaly-detection-pwa
    env: static
    buildCommand: npm run build
    publishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
